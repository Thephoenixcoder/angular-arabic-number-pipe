import { ArabicNumTransPipe } from './arabic-num-trans.pipe';

describe('ArabicNumTransPipe', () => {
  it('create an instance', () => {
    const pipe = new ArabicNumTransPipe();
    expect(pipe).toBeTruthy();
  });
});
